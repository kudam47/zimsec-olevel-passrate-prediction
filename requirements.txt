# =============================================================================
# ZIMSEC O-Level Predictive Analytics — Python Dependencies
# =============================================================================
# Author: Cesario Machinga
# Tested on: Python 3.10+ (Windows / macOS / Linux)
#
# Install:
#   pip install -r requirements.txt
#
# Or via the launcher:
#   run_project.bat
# =============================================================================

# --- Core scientific stack ---------------------------------------------------
numpy>=1.24,<3.0
pandas>=2.0,<3.0
scipy>=1.10
scikit-learn>=1.3,<2.0

# --- Machine learning --------------------------------------------------------
# NOTE: shap>=0.46 is REQUIRED to read base_score from xgboost>=2.1 models.
# Older shap versions crash with: "could not convert string to float: '[4.0E1]'"
xgboost>=2.0
shap>=0.46

# --- Statistical analysis (used in EDA / regression diagnostics) ------------
statsmodels>=0.14

# --- Visualisation -----------------------------------------------------------
matplotlib>=3.7
seaborn>=0.13
plotly>=5.17

# --- FastAPI webapp ----------------------------------------------------------
# NOTE: fastapi>=0.110 ensures Starlette>=0.29, which is required for the
# positional TemplateResponse(request, "...", {...}) signature used in main.py.
fastapi>=0.110
uvicorn[standard]>=0.24
jinja2>=3.1
python-multipart>=0.0.6
itsdangerous>=2.1

# --- Model persistence -------------------------------------------------------
joblib>=1.3

# --- Notebook / Jupyter ------------------------------------------------------
# Needed for running the .ipynb and for HTML export via nbconvert
jupyter>=1.0
ipykernel>=6.25
nbconvert>=7.10
notebook>=7.0

# --- Optional: PDF export (used by /forecast/export-pdf in main.py) ---------
# Uncomment if you need PDF download from the Forecast page.
# reportlab>=4.0
